This is a [Next.js](https://nextjs.org) project bootstrapped with [`create-next-app`](https://github.com/vercel/next.js/tree/canary/packages/create-next-app).

## Getting Started

First, run the development server:

```bash
npm run dev

tamplets : tamplets are similar to the layouts which is used to wrap the child components or page , but with a tamplate when a user navigates between different routes that share a
            share a comman tamlate a new instance of the component is mounted , dom element are recreated , states are not presserverd . 